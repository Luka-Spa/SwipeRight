apiVersion: v1
kind: Service
metadata:
  name: profile
  # annotations:
  #   konghq.com/plugins: swiperight-jwt
spec:
  selector:
    app: profile
  ports:
  - name: http
    protocol: TCP
    port: 8080
